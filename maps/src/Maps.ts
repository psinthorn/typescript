export class Maps {

    // create properties with types
    // สร้าง property และกำหนด types ให้กับ properties


    // use constructor to initial data to each properties
    // ใช้ construcotr ในการกำหนดค่าเริ่มต้นให้กับ properties โดนใช้ faker
};